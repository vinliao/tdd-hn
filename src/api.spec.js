// jest.mock('@/api')
import api from '@/api'

describe('api', () => {
  it('i dont know yet', async () => {
    // api.get = jest.fn(() => true)
    // const response = await api.get('news', '1.json')
  })
})